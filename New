gSystem.Text.StringBuilder sb = new System.Text.StringBuilder();
foreach (DataColumn column in newRow.Table.Columns)
{
    sb.AppendFormat("{0}={1}, ", column.ColumnName, newRow[column] ?? "NULL");
}
log.Info("NewRow Data: " + sb.ToString().TrimEnd(',', ' '));
