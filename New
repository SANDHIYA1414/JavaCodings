sdir ="\0"
internal static bool bDirExists(string sDir, bool bCreate = true)
		{
			bool result = false;
			Object fso = new Object();

			try
			{


				if (!Directory.Exists(sDir))
				{
					if (bCreate)
					{
						Directory.CreateDirectory(sDir);
						return true;
					}
					else
					{
						return false;
					}
				}
				else
				{
					return true;
				}
			}
			catch (Exception ex)
			{
				log.Error("An Error has occured", ex);
				//SetStatus -1, "Unable to Create or Confirm: " & sDir, 0
				result = false;
			}
			return result;
		}
