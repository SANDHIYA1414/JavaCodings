
bool hasOtherHQs = false;
string[] arrHQs = Headquarters_Information.Split(',');

foreach (string hq in arrHQs)
{
    if (hq.Trim() != "All Available Headquarters")
    {
        hasOtherHQs = true;
        break;
    }
}
